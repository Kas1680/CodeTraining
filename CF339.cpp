#include<bits\stdc++.h>
using namespace std;

int main(){
    string input;
    cin >> input;
    
    int k = input.length();
    int a[k];
    for(int i = 0; i < input.length(); i++){
        if(input[i] != '+'){

        }
    }
}